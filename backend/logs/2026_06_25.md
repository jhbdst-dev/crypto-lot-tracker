# 2026-06-25

## 오늘 목표

거래내역을 `BUY`와 `SELL`로 구분하고,  
업비트 WebSocket 현재가를 연결할 준비를 한다.

---

## PHASE 3: BUY / SELL 거래 분기

### 작업 이유

DB에는 매수와 매도 거래가 함께 저장될 수 있다.  
그래서 계산할 때 `trade_type`을 기준으로 거래를 구분해야 한다.

### 작업 파일

- `calculator.py`

### 작업 함수

- `calculate_asset()`

### 작업 내용

- [✅] `row[3]`에서 `trade_type` 꺼내기
- [✅] `trade_type == "BUY"`이면 기존 매수 계산 실행
- [✅] `trade_type == "SELL"` 분기 추가 (`pass`)
- [✅] 기존 출력 결과가 깨지지 않는지 확인
- [✅] 커밋

---

### 매도 데이터 추가

```sql
INSERT INTO trades (
    coin,
    market,
    trade_type,
    price,
    quantity,
    fee,
    settlement_amount,
    executed_at,
    trade_amount
)
VALUES (
    'XRP',
    'KRW',
    'SELL',
    2102,
    237.86869647,
    249.99,
    499749,
    NOW(),
    499999
);
```

### 작업 내용

- [✅] DB에 `SELL` 테스트 거래 추가
- [✅] `calculate_asset()`에서 `trade_type == "SELL"` 분기 추가
- [✅] `sell_trade_results` 리스트 생성
- [✅] `SELL` 거래 데이터를 `sell_trade_results`에 저장
- [✅] `sell_trade_results`를 `return`에 추가
- [✅] `main.py`에서 `sell_trade_results` 출력
- [✅] DB의 `SELL` 거래가 정상 출력되는지 확인
- [✅] 커밋

---
